# FlightStrips

FlightStrips is an Electron application which integrates with EuroScope to provide virtual flight strips mimicking the NITOS system.

FlightStrips consists of three components: the Electron application, the EuroScope plugin and the C# backend.
