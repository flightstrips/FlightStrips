import React from 'react'; // we need this to make JSX compile
import { Button } from '../shared/button';



export function PlannedFlightBtn( title : any) {
    return(
        <>
            <Button title="PLANNED"/>
        </>
    );
}